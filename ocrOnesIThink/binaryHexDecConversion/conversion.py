#!/usr/bin/env python

usrInput = int(input("Please enter the number: "))
format = input("Specify format: b for binary, d for decimal and h for hex: ")
convert = input("What do you want to convert it to (same paramaters as above): ")

def toBi():
    divWith = usrInput
    onHndrdTwntyEigt = divWith / 128
    if onHndrdTwntyEigt == 1: 
    
    sxfs = divWith 
def toDec():
    if format.lower().strip() == "b":

    elif format.lower().strip() == "h":
        divSxns = int(usrInput / 16)
        divRemainder

