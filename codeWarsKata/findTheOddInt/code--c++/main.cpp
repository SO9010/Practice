#include <vector>
#include <iostream>

/*Sample input:

int findOdd(const std::vector<int>& numbers){
    for(int i = sizeof(numbers); i > 0; i--){ //learnt about sizeof() i only knew the one for strings :/
        //box i has numbers in. We will loop while it is bigger than 0. i > 0 
    }
}*/
int main(){
    while(true){
        std::cout << "TRUE";
    }
    return 0;
}